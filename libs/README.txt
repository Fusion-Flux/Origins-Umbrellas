The way you're supposed to depend on Origins is through JitPack, which is a complete nightmare even when it's functional.
The version of Origins we need is just not available, so we have to do this garbage.
Each JiJ needs to be manually extracted since loom doesn't load them in dev.
